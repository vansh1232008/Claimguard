# ---- core (everything below runs offline, no API keys, no services) ----
fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
pydantic==2.9.2
pydantic-settings==2.5.2
python-multipart==0.0.9
httpx==0.27.2
numpy==2.1.1
pandas==2.2.3
scikit-learn==1.5.2
xgboost==2.1.1
networkx==3.3

# ---- agent orchestration ----
langgraph==0.2.28

# ---- tests ----
pytest==8.3.3

# ---- optional upgrades -------------------------------------------------
# Install these to swap a subsystem from its offline fallback to the real
# backend. Nothing breaks if they are absent.
#
#   qdrant-client==1.11.2          # policy RAG on a real vector database
#   neo4j==5.24.0                  # relationship graph in Neo4j
#   sentence-transformers==3.1.1   # dense embeddings instead of hashed ones
#   psycopg2-binary==2.9.9         # PostgreSQL instead of SQLite
#   opencv-python-headless==4.10.0.84  # classical damage analysis
#   ultralytics==8.3.0             # YOLOv8 damage detection
#   pytesseract==0.3.13            # OCR for scanned invoices
#   pdfminer.six==20240706         # text extraction from PDF documents
#   pillow==10.4.0                 # image handling for the vision fallbacks
